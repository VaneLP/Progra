import java.util.Scanner;

public class dawdawd {
    /* crea una funcion que lea una cadena y compruebe si contiene un numero
    o no, y devuelva num entero en caso de ser num. Asegurate de que el programa no de errores,
    independientemente de la entrada introducida */
    public static boolean Alex_Te_Quiero_MUCHO(){
        return true;
    }

    public static int s(){
        Scanner entrada = new Scanner(System.in);
         if (entrada.hasNextInt()){
             return Integer.parseInt(entrada.next());
         }
        return 0;
    }

    public static void main(String[] args) {
        System.out.println(Alex_Te_Quiero_MUCHO());
    }
}
